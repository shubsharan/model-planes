---
description: Detect Git remote URL for GitHub integration
---

Read and follow the workflow skill at `.claude/skills/speckit-git-remote/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
