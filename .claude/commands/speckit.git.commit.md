---
description: Auto-commit changes after a Spec Kit command completes
---

Read and follow the workflow skill at `.claude/skills/speckit-git-commit/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
