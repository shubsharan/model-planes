---
description: Initialize a Git repository with an initial commit
---

Read and follow the workflow skill at `.claude/skills/speckit-git-initialize/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
