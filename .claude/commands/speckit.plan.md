---
description: Execute the implementation planning workflow using the plan template to generate design artifacts.
---

Read and follow the workflow skill at `.claude/skills/speckit-plan/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
