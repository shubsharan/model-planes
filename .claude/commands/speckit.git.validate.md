---
description: Validate the feature/NNNN-name branch and matching feature directory.
---

Read and follow the workflow skill at `.claude/skills/speckit-git-validate/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
