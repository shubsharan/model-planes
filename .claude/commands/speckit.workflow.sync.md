---
description: 'Spec-kit workflow command: speckit-workflow-sync'
---

Read and follow the workflow skill at `.claude/skills/speckit-workflow-sync/SKILL.md` exactly. Treat everything below as the skill's `$ARGUMENTS` input.

$ARGUMENTS
