---
name: speckit-git-validate
description: Validate the feature/NNNN-name branch and matching feature directory.
---

# Validate Feature Branch

Run `.specify/scripts/bash/check-workflow.sh`. The current branch must match
`feature/NNNN-<slug>` and is the source of truth for its feature directory.
