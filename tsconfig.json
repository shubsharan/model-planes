{
  "//": "Root editor config for loose root-level files (e.g. vitest.config.mts). Each package type-checks independently via its own tsconfig.json — there is no project-reference build graph, since packages resolve each other's types directly from source (see tsconfig.base.json: noEmit).",
  "extends": "./tsconfig.base.json",
  "include": ["*.mts", "*.ts"],
  "exclude": ["packages"]
}
